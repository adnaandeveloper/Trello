import { Button } from '@mui/material';
import React from 'react';

const AddTaskButton = () => {
  return (
    <Button sx={{ width: '272px', color: '#ebecf0' }}>
      {' '}
      add another list last{' '}
    </Button>
  );
};

export default AddTaskButton;
